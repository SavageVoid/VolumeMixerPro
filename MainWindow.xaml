<Window x:Class="VolumeMixerPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VolumeMixerPro"
        mc:Ignorable="d"
        Title="Volume Mixer Pro" Height="450" Width="380"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="NoResize" Topmost="True" ShowInTaskbar="False"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        RenderOptions.BitmapScalingMode="HighQuality"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        Visibility="Collapsed" Opacity="0" Background="Transparent">
    <Window.Resources>
        <Style x:Key="AppSliderStyle" TargetType="Slider">
            <Setter Property="Background" Value="#2A2F36"/>
            <Setter Property="Foreground" Value="#4CAF50"/>
            <Setter Property="Height" Value="20"/>
        </Style>
    </Window.Resources>
    <Border BorderThickness="0" CornerRadius="12">
        <Border.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF1C2128" Offset="0"/>
                <GradientStop Color="#FF0F1419" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="1"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="1"/>
                <RowDefinition Height="45"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#1C2128" CornerRadius="12,12,0,0">
                <Grid>
                    <TextBlock Text="🔊  Volume Mixer Pro" Foreground="White" 
                               FontSize="16" FontWeight="SemiBold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Width="30" Height="30" Margin="0,0,5,0"
                                Style="{StaticResource ActionButtonStyle}" Click="SettingsButton_Click" ToolTip="Settings">
                            <TextBlock x:Name="SettingsIcon" Text="&#xE713;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="14" RenderTransformOrigin="0.5,0.5">
                                <TextBlock.RenderTransform>
                                    <RotateTransform Angle="0"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </Button>
                        <Button Content="✕" Width="30" Height="30"
                                Style="{StaticResource ActionButtonStyle}" Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Row="1" BorderBrush="#11FFFFFF" BorderThickness="0,0,0,1"/>
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel Name="SessionsContainer" Margin="15,10"/>
            </ScrollViewer>
            <Border Grid.Row="3" BorderBrush="#11FFFFFF" BorderThickness="0,1,0,0"/>
            <Border Grid.Row="4" Background="#1C2128" CornerRadius="0,0,12,12">
                <TextBlock x:Name="FooterText" Text="Ctrl + Scroll  •  Ctrl + Alt + V  •  Ctrl + Alt + M" 
                           Foreground="#B8BCC8" FontSize="10"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Window>
